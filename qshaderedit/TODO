
- display line and column numbers to status bar.

Parameter panel:
- Fix problem with boolean value:
  QComboBox::setProperty("currentItem", value) failed: property invalid, read-only or does not exist
  QComboBox::property("currentItem") failed: property invalid, read-only or does not exist
- add color picker for color parameters.

Textures:
- use texture objects instead of resource manager.
- add support for multiple image plugins.
- add dds image plugin.

Effects:
- complete cgfx support -> set parameter values, preserve parameter values after rebuild, texture/sampler parameters.
- add support for texture parameters.
- add glfx support.
- integrated help.

Performance panel:
- add effect performance interface.
- add performance panel to display performance info.
- use NVShaderPerf.

Scene:
- add more shapes.
- add support for geometry file formats and simple scenes.
- compute normals, tangents, binormals.

KDE integration:
- port to KDE4.
- add icons.
- use ktexteditor interface.
- add support for autocompletion and syntax highlight.
- highlight errors.


